{
  "name": "purescript-yoga-stories",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "spago build && bun run bundle",
    "bundle": "bun build output/YogaStories.UI.Client/index.js --outfile dist/client.js --format esm",
    "test": "spago test",
    "start": "bun run build && bun -e 'import { main } from \"./output/Main/index.js\"; main();'"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "leva": "^0.9.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}
